<?php

App::uses('AppModel', 'Model');

/**
 * CakePHP UserModel
 * @author Go! Ponto a ponto
 */
class RedesUser extends AppModel {

    public $belongsTo = array('User');    

    public $virtualFields = array(
//        'ativo' => "CASE WHEN User.active = 1 THEN 'Sim' ELSE 'Não' END",
 //       'grupo' => "select name from groups g where g.id = User.group_id",
  //      'totalNotificacoes' => "select count(id) from avisos_users au where au.user_id = User.id and visto <> 1"
    );
    
}